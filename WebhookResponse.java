package com.example.demo;

// Make sure field names match the JSON response exactly
public class WebhookResponse {
    private String webhookUrl;
    private String accessToken;

    // Add getters and setters for both fields
}